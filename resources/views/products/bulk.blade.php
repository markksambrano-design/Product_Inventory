@extends('layouts.app') @section('title','Bulk Product Tools') @section('content')
<div class="mb-4"><h2>Bulk Product Tools</h2><p class="text-muted">Update multiple products or import a CSV file.</p></div><div class="row g-4 mb-4"><div class="col-lg-6"><div class="card shadow-sm h-100"><div class="card-body"><h5><i class="bi bi-file-earmark-arrow-up text-primary"></i> Import Products</h5><p class="text-muted small">New codes are created; matching codes are updated. Imported products start with zero stock.</p><form method="POST" action="{{ route('products.bulk.import') }}" enctype="multipart/form-data">@csrf<input type="file" name="csv" accept=".csv" class="form-control mb-3" required><button class="btn btn-primary">Import CSV</button> <a href="{{ route('products.bulk.template') }}" class="btn btn-outline-secondary">Download Template</a></form></div></div></div><div class="col-lg-6"><div class="card shadow-sm h-100"><div class="card-body"><h5><i class="bi bi-info-circle text-primary"></i> Bulk Update Guide</h5><p class="text-muted small">Select products below, then apply a price percentage, category, or active status. Empty fields remain unchanged.</p></div></div></div></div>
<form method="POST" action="{{ route('products.bulk.update') }}">@csrf<div class="card shadow-sm"><div class="card-header bg-white"><div class="row g-2"><div class="col-md-3"><input name="price_percent" type="number" step=".01" class="form-control" placeholder="Price adjustment %"></div><div class="col-md-3"><select name="category_id" class="form-select"><option value="">Keep category</option>@foreach($categories as $c)<option value="{{ $c->id }}">{{ $c->name }}</option>@endforeach</select></div><div class="col-md-3"><select name="status" class="form-select"><option value="">Keep status</option><option value="active">Active</option><option value="inactive">Inactive</option></select></div><div class="col-md-3"><button class="btn btn-primary w-100">Apply to Selected</button></div></div></div><div class="card-body table-responsive"><table class="table align-middle"><thead><tr><th><input type="checkbox" id="all"></th><th>Code</th><th>Product</th><th>Category</th><th>Price</th><th>Status</th></tr></thead><tbody>@foreach($products as $p)<tr><td><input type="checkbox" name="product_ids[]" value="{{ $p->id }}"></td><td>{{ $p->product_code }}</td><td>{{ $p->product_name }}</td><td>{{ $p->category_label }}</td><td>&#8369;{{ number_format((float)$p->selling_price,2) }}</td><td>{{ $p->status?'Active':'Inactive' }}</td></tr>@endforeach</tbody></table></div></div></form><script>document.getElementById('all').onchange=e=>document.querySelectorAll('[name="product_ids[]"]').forEach(x=>x.checked=e.target.checked)</script>
@endsection
