@extends('layouts.app') @section('title','Purchase Orders') @section('content')
<div class="d-flex justify-content-between align-items-center mb-4"><div><h2>Purchase Orders</h2><p class="text-muted mb-0">Order and receive supplier inventory.</p></div><a href="{{ route('purchase-orders.create') }}" class="btn btn-primary"><i class="bi bi-plus-lg"></i> New Purchase Order</a></div>
<div class="card shadow-sm"><div class="card-body table-responsive"><table class="table align-middle"><thead><tr><th>PO Number</th><th>Supplier</th><th>Order Date</th><th>Expected</th><th>Total</th><th>Status</th><th></th></tr></thead><tbody>@forelse($orders as $order)<tr><td><strong>{{ $order->po_number }}</strong></td><td>{{ $order->supplier->supplier_name }}</td><td>{{ $order->order_date->format('M d, Y') }}</td><td>{{ $order->expected_date?->format('M d, Y')??'-' }}</td><td>&#8369;{{ number_format((float)$order->total_amount,2) }}</td><td><span class="badge {{ $order->status==='received'?'bg-success':($order->status==='cancelled'?'bg-secondary':'bg-primary') }}">{{ ucfirst($order->status) }}</span></td><td><a href="{{ route('purchase-orders.show',$order) }}" class="btn btn-sm btn-outline-primary">View</a></td></tr>@empty<tr><td colspan="7" class="text-center text-muted py-5">No purchase orders yet.</td></tr>@endforelse</tbody></table>{{ $orders->links() }}</div></div>
@endsection
