@extends('layouts.app')
@section('title', 'Inventory Ledger')
@section('content')
<div class="d-flex justify-content-between align-items-center mb-3"><h2>Inventory Ledger</h2><span class="badge text-bg-warning">{{ $mismatches->count() }} reconciliation issue(s)</span></div>
<form class="row g-2 mb-4" method="GET"><div class="col-md-5"><select name="product_id" class="form-select"><option value="">All products</option>@foreach($products as $product)<option value="{{ $product->id }}" @selected(request('product_id') == $product->id)>{{ $product->product_name }}</option>@endforeach</select></div><div class="col-auto"><button class="btn btn-outline-primary">Filter</button></div></form>
@if($mismatches->isNotEmpty())<div class="alert alert-warning"><strong>Product and batch quantities do not match:</strong> @foreach($mismatches as $product){{ $product->product_name }} ({{ $product->quantity }} vs {{ $product->batches_sum_quantity ?? 0 }})@if(!$loop->last), @endif @endforeach</div>@endif
<div class="table-responsive"><table class="table"><thead><tr><th>Date</th><th>Product</th><th>Type</th><th>Quantity</th><th>Batch</th><th>User</th><th>Source</th></tr></thead><tbody>@forelse($movements as $movement)<tr><td>{{ $movement->movement_date?->format('Y-m-d') }}</td><td>{{ $movement->product->product_name }}</td><td>{{ ucfirst(str_replace('_', ' ', $movement->movement_type)) }}</td><td class="{{ $movement->quantity < 0 ? 'text-danger' : 'text-success' }}">{{ $movement->quantity > 0 ? '+' : '' }}{{ $movement->quantity }}</td><td>{{ $movement->productBatch?->batch_number ?: 'N/A' }}</td><td>{{ $movement->user?->name ?: 'System' }}</td><td>{{ class_basename($movement->source_type) }} #{{ $movement->source_id }}</td></tr>@empty<tr><td colspan="7">No inventory movements recorded yet.</td></tr>@endforelse</tbody></table></div>{{ $movements->links() }}
@endsection