@extends('layouts.app')
@section('title','Notifications')
@section('content')
<div class="d-flex justify-content-between align-items-center mb-4"><div><h2>Notifications</h2><p class="text-muted mb-0">Inventory alerts and system updates.</p></div><div class="d-flex gap-2"><form method="POST" action="{{ route('notifications.preferences') }}">@csrf<label class="form-check form-switch"><input class="form-check-input" type="checkbox" name="email_enabled" value="1" onchange="this.form.submit()"><span class="form-check-label">Email alerts</span></label></form><form method="POST" action="{{ route('notifications.read-all') }}">@csrf<button class="btn btn-outline-primary">Mark all as read</button></form></div></div>
<div class="card shadow-sm"><div class="list-group list-group-flush">@forelse($notifications as $notification)<form method="POST" action="{{ route('notifications.read',$notification) }}">@csrf<button class="list-group-item list-group-item-action d-flex gap-3 py-3 w-100 text-start {{ $notification->read_at?'':'bg-primary-subtle' }}"><i class="bi {{ $notification->type==='expiration'?'bi-calendar-x':'bi-exclamation-triangle' }} fs-4 text-primary"></i><span><strong>{{ $notification->title }}</strong><br><small class="text-muted">{{ $notification->message }} · {{ $notification->created_at->diffForHumans() }}</small></span></button></form>@empty<div class="p-5 text-center text-muted"><i class="bi bi-bell-slash fs-2 d-block mb-2"></i>No notifications.</div>@endforelse</div></div><div class="mt-3">{{ $notifications->links() }}</div>
@endsection
